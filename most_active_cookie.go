package main

import (
	"encoding/csv"
	"log"
	"strings"
)

func GetCookiesWithinTimestamp(reader *csv.Reader, timestamp string) ([]string, error) {

}

func GetMostActiveCookies(records []string) ([]string, error) {

}